# Новая структура компонентов

## Обзор

Мы рефакторили структуру компонентов, чтобы сделать её более организованной и масштабируемой, подобно Storybook. Каждый компонент теперь имеет свою папку с файлами компонента, конфигурацией sandbox и документацией.

## Структура

```
src/components/
├── badge/
│   ├── badge.tsx          # Основной компонент
│   ├── sandbox.config.ts  # Конфигурация для sandbox
│   ├── docs.md           # Документация
│   └── index.ts          # Экспорты
├── button/
│   ├── button.tsx
│   ├── sandbox.config.ts
│   ├── docs.md
│   └── index.ts
└── ... (другие компоненты)
```

## Файлы компонента

### 1. Основной компонент (`component.tsx`)
- Содержит сам компонент React
- Включает все типы, интерфейсы и варианты
- Экспортирует компонент и его варианты

### 2. Конфигурация Sandbox (`sandbox.config.ts`)
- Определяет варианты для sandbox
- Содержит функцию `generateVariants`
- Экспортирует конфигурацию

### 3. Документация (`docs.md`)
- Описание компонента
- Примеры использования
- Список пропсов
- Лучшие практики

### 4. Индексный файл (`index.ts`)
- Экспортирует компонент
- Экспортирует конфигурацию sandbox

## Утилиты

### Component Collector (`src/lib/component-collector.ts`)
- Сканирует папки компонентов
- Собирает информацию о файлах
- Валидирует структуру

### Component Registry (`src/lib/component-registry.ts`)
- Загружает компоненты динамически
- Предоставляет API для доступа к компонентам
- Кэширует загруженные компоненты

### New Sandbox Config (`src/lib/new-component-sandbox-config.ts`)
- Работает с новой структурой
- Генерирует варианты для sandbox
- Предоставляет сводку конфигураций

## Скрипты

### Миграция компонентов (`scripts/migrate-components.ts`)
- Автоматически перемещает компоненты
- Создает базовые файлы конфигурации
- Генерирует документацию

### Обновление импортов (`scripts/update-imports.ts`)
- Обновляет импорты в существующих файлах
- Заменяет старые пути на новые

### Тестирование структуры (`scripts/test-new-structure.ts`)
- Проверяет целостность структуры
- Генерирует отчеты
- Валидирует компоненты

## Преимущества новой структуры

1. **Организация**: Каждый компонент изолирован в своей папке
2. **Масштабируемость**: Легко добавлять новые компоненты
3. **Документация**: Встроенная документация для каждого компонента
4. **Sandbox**: Автоматическая генерация вариантов
5. **Типизация**: Полная поддержка TypeScript
6. **Автоматизация**: Скрипты для миграции и обновления

## Использование

### Импорт компонента
```tsx
import { Badge } from "@/components/badge"
import { Button } from "@/components/button"
```

### Получение конфигурации sandbox
```tsx
import { getNewSandboxConfig } from "@/lib/new-component-sandbox-config"

const config = getNewSandboxConfig('badge')
```

### Работа с реестром компонентов
```tsx
import { getComponentRegistry } from "@/lib/component-registry"

const registry = getComponentRegistry()
const components = registry.getAllComponents()
```

## Миграция

1. **Автоматическая миграция**: Используйте `scripts/migrate-components.ts`
2. **Обновление импортов**: Запустите `scripts/update-imports.ts`
3. **Тестирование**: Проверьте структуру с помощью `scripts/test-new-structure.ts`

## Добавление нового компонента

1. Создайте папку для компонента
2. Добавьте файлы: `component.tsx`, `sandbox.config.ts`, `docs.md`, `index.ts`
3. Зарегистрируйте компонент в реестре
4. Обновите импорты при необходимости

## Статус миграции

✅ **Завершено:**
- Создание новой структуры папок
- Миграция всех компонентов
- Создание конфигураций sandbox
- Генерация документации
- Обновление импортов
- Создание утилит

🔄 **В процессе:**
- Тестирование в реальном приложении
- Оптимизация производительности

📋 **Планируется:**
- Автоматическая генерация компонентов
- Интеграция с CI/CD
- Расширенная валидация
