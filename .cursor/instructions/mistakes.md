# 🧠 Частые ошибки ИИ при написании кода на React

## ⚛️ Логика и состояние
1. Дублирование состояния или хранение вычисляемых значений в `useState`.
2. Бесконечные рендеры из-за неправильных зависимостей в `useEffect`.
3. Пропуск нужных зависимостей в `useEffect`.
4. Неверное использование хуков (вызов внутри условий, циклов, вне компонента).
5. Неправильная работа с формами и controlled components.
6. Отсутствие `useMemo`/`useCallback`, что вызывает лишние ререндеры.
7. Ошибки с `ref` — использование до монтирования или прямое изменение DOM.

## 🔁 Работа с данными и API
8. Вызовы API прямо в теле компонента без `useEffect`.
9. Необработанные ошибки запросов и гонки состояний.
10. Использование асинхронного кода без `await` или `return`.

## 🧩 Пропсы и компоненты
11. Передача несуществующих или лишних пропсов.
12. Использование индексного key в списках (`key={index}`).
13. Смешение логики и UI в одном компоненте, без декомпозиции.
14. Ошибки с импортами (`default` vs `named`, ESM vs CJS).

## 🧭 Навигация и контекст
15. Неправильное использование `Link` / `useNavigate` / Next.js router.
16. Ошибки в Context API — лишние ререндеры без `memo` или неверная вложенность.
17. Использование client-хуков в server-компонентах (Next.js 13+).

## 🎨 Структура и стили
18. Конфликты `className`, inline- и Tailwind-стилей.
19. Несогласованность структуры проекта (папки, компоненты, shared UI).

## 🧾 Типизация и обработка ошибок
20. Отсутствие строгих типов пропсов и стейта.
21. Неверная типизация компонентов (например, без `React.FC`).
22. Отсутствие Error Boundaries и `try/catch` при async-операциях.

## ⚙️ SSR и сборка
23. Использование браузерных API (`window`, `document`) на сервере.
24. Конфликты при сборке из-за неверных импортов или зависимостей.
25. Плохое понимание различий между client/server рендерингом в Next.js.

---

💡 **Как избежать ошибок**
- Проверяй зависимости `useEffect` и структуру стейта.  
- Добавляй `key`, мемоизацию и типы.  
- Разделяй логику и представление.  
- Тестируй компоненты изолированно.  
- Проверяй, в каком окружении работает код (client/server).
