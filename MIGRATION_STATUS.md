# Статус выполнения плана миграции на Stitches

## ✅ Фаза 1: Генератор токенов из Figma - **ЗАВЕРШЕНА**

### 1.1. Структура генератора ✅
- ✅ `scripts/generate-tokens.ts` - основной скрипт генерации
- ✅ `scripts/token-parser.ts` - парсинг export.json
- ✅ `scripts/token-resolver.ts` - разрешение зависимостей
- ✅ `scripts/css-generator.ts` - генерация CSS
- ✅ `scripts/ts-generator.ts` - генерация TypeScript типов

### 1.2. Парсинг export.json ✅
- ✅ Загружает `export.json` из `.cursor/instructions/export.json`
- ✅ Обрабатывает все коллекции с режимами
- ✅ Извлекает токены с путями и значениями
- ✅ Определяет зависимости

### 1.3. Разрешение зависимостей ✅
- ✅ Реализован `TokenResolver` класс
- ✅ Разрешает токены для всех комбинаций режимов
- ✅ Обрабатывает зависимости типа `{xBase.400}`

### 1.4. Генерация tokens.css ✅
- ✅ Генерирует CSS переменные с режимами
- ✅ Поддерживает `data-xbase-mode` и `data-semantic-mode`
- ✅ Правильное именование токенов (дефисы вместо точек)
- ✅ Дефолтные режимы установлены

### 1.5. Генерация tokens.ts ✅
- ✅ Генерирует TypeScript типы (`XBaseMode`, `SemanticMode`)
- ✅ Создает `tokenPaths` объект со всеми путями
- ✅ Экспортирует функции `getTokenValue`, `setXBaseMode`, `setSemanticMode`, `getXBaseMode`, `getSemanticMode`, `initializeTokenModes`

### 1.6. Запуск генератора ✅
- ✅ Команда `npm run generate:tokens` настроена в `package.json`
- ✅ Выходные файлы: `src/styles/tokens.css` и `src/styles/tokens.ts`

---

## ✅ Фаза 2: Настройка Stitches - **ЗАВЕРШЕНА**

### 2.1. Установка зависимостей ✅
- ✅ `@stitches/react` установлен (версия 1.2.8)
- ✅ `tsx` установлен в devDependencies (версия 4.20.6)

### 2.2. Создание stitches.config.ts ✅
- ✅ Файл создан: `src/styles/stitches.config.ts`
- ✅ Настроены токены для:
  - ✅ `space` (xBase токены + gravity system)
  - ✅ `sizes` (дублирование space)
  - ✅ `fontSizes` (body, headline, display, vtbGroup, omegaUI)
  - ✅ `lineHeights` (body, headline, display)
  - ✅ `fonts` (sans, display, headline, body)
  - ✅ `fontWeights` (regular, medium, semibold, bold + токены)
  - ✅ `colors` (brand, semantic, button компоненты - частично)
  - ✅ `radii` (xBase + gravity system)
- ✅ Настроены `media` breakpoints
- ✅ Настроены `utils` (px, py, p, mx, my, m, gap)

### 2.3. Интеграция с Next.js ✅
- ✅ `src/app/layout.tsx` содержит `getCssText()` интеграцию
- ✅ Stitches стили инжектируются в `<head>` через `dangerouslySetInnerHTML`

### 2.4. Подключение tokens.css ✅
- ✅ `src/styles/globals.css` импортирует `./tokens.css`
- ✅ Tailwind импорты удалены

### 2.5. Утилиты для переключения режимов ✅
- ✅ `src/lib/token-utils.ts` создан
- ✅ Реэкспортирует функции из `tokens.ts`
- ✅ Автоматическая инициализация режимов при загрузке

### 2.6. Обновление globals.css ✅
- ✅ Tailwind импорты удалены
- ✅ Оставлены только `@font-face` и базовые стили

---

## 🟡 Фаза 3: Миграция компонентов - **В ПРОЦЕССЕ**

### 3.1. Порядок миграции компонентов

#### Базовые компоненты:
- 🟡 `Button` - **В ПРОЦЕССЕ** (создан `button.tsx` на Stitches, но не завершен)
- ❌ `Text` - не начат
- ❌ `Input` - не начат
- ❌ `Label` - не начат

#### Средние компоненты:
- ❌ `Badge` - не начат
- ❌ `Avatar` - не начат
- ❌ `Cell` - не начат
- ❌ `Separator` - не начат
- ❌ `Skeleton` - не начат

#### Сложные компоненты:
- ❌ `Dialog` - не начат
- ❌ `Sheet` - не начат
- ❌ `DropdownMenu` - не начат
- ❌ `Select` - не начат
- ❌ `Switch` - не начат
- ❌ `Tooltip` - не начат
- ❌ `Collapsible` - не начат

#### Специальные компоненты:
- ❌ `Icon` - не начат
- ❌ `Image` - не начат
- ❌ `ButtonIcon` - не начат
- ❌ `Textarea` - не начат

### Статус Button компонента:
- ✅ Создан `ButtonContainer` (базовый компонент)
- ✅ Создан `ButtonStyled` (расширяет Container)
- ✅ Создан React компонент `Button`
- 🟡 Реализованы варианты: `filled` (частично)
- ❌ Не реализованы варианты: `outlined`, `tonned`, `transparent`
- ❌ Не все `colorScheme` варианты завершены
- ❌ Не все disabled состояния реализованы
- ❌ Не добавлены недостающие токены в `stitches.config.ts`

---

## ❌ Фаза 4: Очистка - **НЕ НАЧАТА**

### 4.1. Удаление зависимостей ❌
- ❌ `tailwindcss` все еще установлен
- ❌ `@tailwindcss/postcss` все еще установлен
- ❌ `tw-animate-css` все еще установлен
- ❌ `tailwind-merge` все еще установлен
- ❌ `class-variance-authority` все еще установлен

### 4.2. Удаление конфигурационных файлов ❌
- ❌ `postcss.config.mjs` не обновлен (Tailwind плагин не удален)
- ❌ `tailwind.config.*` не проверен/удален

### 4.3. Обновление postcss.config.mjs ❌
- ❌ Tailwind плагин не удален

### 4.4. Удаление старых токенов ❌
- ✅ `src/styles/tokens.css` заменен на сгенерированный из Figma
- ❌ `src/styles/colors.css` не проверен/удален

### 4.5. Обновление импортов ❌
- ❌ Использование `cn()` из `@/lib/utils` не проверено
- ❌ Неиспользуемые импорты не удалены

### 4.6. Финальная проверка ❌
- ❌ `npm run build` не запущен
- ❌ Визуальная проверка не выполнена
- ❌ Переключение режимов не протестировано

---

## 📊 Общий прогресс

- **Фаза 1:** ✅ 100% завершена
- **Фаза 2:** ✅ 100% завершена
- **Фаза 3:** 🟡 ~5% завершена (только Button начат)
- **Фаза 4:** ❌ 0% завершена

**Общий прогресс миграции: ~51%**

---

## 🔧 Что нужно сделать дальше

### Приоритет 1: Завершить Button компонент
1. Добавить недостающие токены в `stitches.config.ts`:
   - `transparent-primary`, `neutral-32`, `neutral-40`
   - `constant-32`, `constant-40`, `constant-70`, `primary-70`
   - Все токены для `outlined`, `tonned`, `transparent` вариантов
2. Завершить реализацию всех вариантов Button
3. Добавить все disabled состояния
4. Протестировать компонент

### Приоритет 2: Мигрировать остальные базовые компоненты
1. `Text` - использует типографику
2. `Input` - использует spacing, radii, colors
3. `Label` - простой компонент

### Приоритет 3: Очистка (после миграции всех компонентов)
1. Удалить Tailwind зависимости
2. Обновить конфигурации
3. Финальная проверка

